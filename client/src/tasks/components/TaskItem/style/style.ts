import styled from "styled-components";
import {InputGroup} from "react-bootstrap";

export const TaskGroup = styled(InputGroup)`
  width: 600px;
  margin-bottom: 10px;
  display: flex;
  justify-content: space-between;
`;